# icey-packages

Static package repositories for Icey distributions.

Current repositories:

- `icey/`: APT repository root for `icey-server`

This repository is intended to be served as a static site. The package
contents are published automatically from `nilstate/icey-cli`.
